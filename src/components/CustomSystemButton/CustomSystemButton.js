import React from 'react';
import './CustomSystemButton.scss';

const CustomSystemButton = ({ category }) => {
  return <div className={`system-button ${category}`}></div>;
};

export default CustomSystemButton;
